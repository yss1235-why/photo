[build]
  command = "bun install && bun run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
```

**OR** update your build command in the Netlify UI to:
```
bun install && bun run build
